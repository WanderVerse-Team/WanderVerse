### 2.3 Testing functional requirements

| Test Case No. | Test Case Description | Expected Outcome | Actual Outcome | Status |
| :--- | :--- | :--- | :--- | :--- |
| **FR 1** | The system must present game content derived from 1st term lessons in the official Grade 3 Maths syllabus. | Game levels contain content covering Counting, Numbers 1–99, Addition, Subtraction, Fractions, Place Value, Multiplication, Division, Measuring Length, Time, Solid Objects & Shapes, Directions, and Days Sequence as per the Grade 3 Maths syllabus. | LevelData ScriptableObjects (e.g., Level1_1s_Data, Level1_Numbers1-20, Fractions_Level1) contain syllabus-aligned content. The `GameType` enum in `LevelData.cs` maps directly to 1st term lesson areas: Counting, Numbers1, Addition1, Fractions, PlaceValue, DaysSequence, etc. Question data (e.g., `QuestionItem` class) uses Sinhala prompts matching the official syllabus. | Pass |
| **FR 2** | All game levels must be fully playable in a complete offline state. | All gameplay mechanics (answering questions, scoring, timer, win/lose conditions) function without requiring an internet connection. | `BaseLevelController.cs` handles all game logic (scoring, timer, win conditions, mistake tracking) entirely on the client side. `LocalDataManager.cs` saves progress to the device using `Application.persistentDataPath`. `CloudSyncManager.cs` only syncs to Firestore when a connection is available and the user is authenticated — gameplay never depends on connectivity. Guest mode (`InitializeAsGuest`) works fully offline with unencrypted local saves. | Pass |
| **FR 3** | Users must be able to create an account using username and password only, and email optionally. | Users can register and log in using a username and password, with an optional email address, via Firebase Authentication. | Firebase Authentication SDK is integrated (via `firebase-admin` on backend, Firebase Auth SDK in Unity). `authMiddleware.js` verifies Firebase ID tokens on the server. The `CloudSyncManager.cs` uses `FirebaseAuth.DefaultInstance.CurrentUser` for authentication flows. Account creation requires only a username and password — email is accepted but not mandatory. The `PlayerData.cs` model stores `userName` as a core field; no email field is required in the data model. Backend tests confirm profiles are created with `userName` only (no email), and also work correctly when email is optionally provided. Backend endpoints (sync, keys) were successfully tested with valid Firebase tokens. | Pass |
| **FR 4** | The system must save all player progress to the device's local storage. | Player progress (XP, level completion, stars, high scores) is persisted to the device's local file system and survives app restarts. | `LocalDataManager.cs` serializes `PlayerData` to JSON via `JsonUtility.ToJson()`, encrypts it using AES (via `EncryptionUtils`), and writes it to `Application.persistentDataPath/wanderverse_save.dat`. The `SaveGame()` method is called by `CloudSyncManager.SyncProgress()` on every progress change. Guest mode saves to `wanderverse_guest_save.dat` as plain JSON. `LoadGame()` reads and decrypts the file back into a `PlayerData` object on startup. | Pass |
| **FR 5** | The system must provide immediate visual or audio feedback to indicate correct and incorrect answers. | Correct answers trigger a success sound and visual cue; incorrect answers trigger an error sound and visual cue. | `BaseLevelController.cs` invokes `HandleCorrectAnswer()` and `HandleWrongAnswer()` which fire the `OnScoreUpdated` event for UI updates. `AudioManager.cs` provides `PlaySuccess()` and `PlayError()` methods using dedicated `AudioClip` fields (`successSound`, `errorSound`). On level completion, `PlayLevelComplete()` triggers a victory sound. The `WinScreenController.cs` shows an animated win screen with star animations (using DOTween) and XP text feedback. | Pass |
| **FR 6** | Progress will be synced across all devices when an internet connection is available. | When online, player progress is uploaded to the cloud and can be retrieved on any other device logged into the same account. | `CloudSyncManager.SyncProgress()` saves locally first via `LocalDataManager.SaveGame()`, then calls `SaveToFirestoreRoutine()` which uploads XP and the full JSON data to Firestore (`users/{userId}`). On login, `FetchCloudData()` retrieves the cloud profile and compares XP — the higher-XP profile wins. The backend `syncController.js` (`POST /api/sync`) also implements conflict resolution (cloud vs. local by XP comparison). Tested via Postman: sync endpoint returns `SUCCESS` for new profiles (201) and handles conflicts correctly (200 with `CONFLICT` status). | Pass |
| **FR 7** | The system should display a leaderboard based on XP, showing the top 10 players by username, along with the current user's rank regardless of their position. | A global leaderboard displays the top 10 players ranked by XP (username only), and additionally includes the current authenticated user's rank, XP, and username — even if they are not in the top 10. Guest users cannot access the leaderboard. | `leaderboardController.js` queries Firestore `users` collection with `.orderBy('xp', 'desc').limit(10)`, returning only `userName` and `xp` fields per entry. Results are cached for 5 minutes. Additionally, the controller fetches the authenticated user's document and uses a Firestore `.where('xp', '>', userXp).count()` aggregation query to compute their rank. The response includes `currentUser: { userName, xp, rank }` alongside `data` (top 10). The endpoint requires authentication via `verifyToken` middleware — guest users receive a 401. On the Unity side, `CloudSyncManager.FetchLeaderboard()` sends an authenticated `GET` request with a Firebase ID token. `LeaderboardUIController.cs` parses the JSON response, populates 10 leaderboard rows, and displays the current user's rank, username, and XP at the bottom. Backend tests verify: top 10 ordering, user rank computation (e.g., rank 6 when 5 players have higher XP), and 401 rejection for unauthenticated requests. | Pass |
| **FR 8** | A progression system to unlock levels must be implemented. | Levels are locked by default and unlock sequentially as the player completes previous levels. | `CloudSyncManager.IsLevelUnlocked()` checks `PlayerData.levelProgress` for an `isUnlocked` flag per `LevelTracker`. The first level (`Island1_Lesson1`) is always unlocked. `UnlockLevel()` sets `isUnlocked = true` for the next level and saves progress. `GameManager.ProcessLevelCompletion()` is called on level success, which triggers `SetHighscoreForLevel()` to record progress. `LevelSelectButton.cs` gates play behind `EnergyManager.TryConsumeEnergy()` and loads levels via `GameManager.LoadLevel()`. | Pass |
| **FR 9** | A profile dashboard to display user statistics must be implemented. | The dashboard shows the player's username, rank, login streak, subject progress bar, and badges. | `DashboardUIController.cs` reads from `CloudSyncManager.Instance.CurrentData` and populates: username (`data.userName`), rank via `GetRankName()` (Novice Explorer / Junior Wanderer / Master Pathfinder based on XP thresholds), login streak, subject progress bar (`CalculateSubjectProgress()` counts levels with stars > 0), and badge icons (activated based on XP > 500 and currentLevel > 5). | Pass |
| **FR 10** | Allowing the user to turn sound effects and music on/off. | Users can independently control music, SFX, and voice volumes, and toggle mute on/off via a settings UI. | `AudioManager.cs` exposes `SetMasterVolume()`, `SetMusicVolume()`, `SetSFXVolume()`, `SetVoiceVolume()` methods for UI sliders. `ToggleMute()` sets master volume to -80dB (effectively silent). All volume preferences are persisted using `PlayerPrefs` (keys: `MasterVol`, `MusicVol`, `SFXVol`, `VoiceVol`, `IsMuted`) and restored on app launch in `Start()`. | Pass |
| **FR 11** | The mascot guides the player throughout the game's maps. | A mascot character appears before each level with dialogue, voice lines, and poses to guide the player through the game. | `BaseLevelController.cs` implements a full mascot tutorial system: `tutorialPanel`, `dialogueText`, `mascotImage`, `mascotMessages[]`, `mascotVoices[]`, and `mascotPoses[]`. `StartGame()` displays the tutorial panel before the level begins. `ShowCurrentTutorialMessage()` formats dynamic placeholders (`{TARGET}`, `{TIME}`, `{MISTAKES}`) and plays voice clips via `AudioManager.PlayVoiceover()`. `AdvanceTutorial()` cycles through messages, then calls `BeginLevel()` when finished. | Pass |

---

### 2.4 Testing non-functional requirements

| Test Case No. | Test Case Description | Test Case Condition | Expected Outcome | Actual Outcome | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **NFR 1** | Security | System not collecting or storing any personal identifiable information from any user. | The system stores only a username (chosen by the player), XP, and game progress — no real names, addresses, phone numbers, or other PII is collected or stored. | `PlayerData.cs` contains only: `userID` (Firebase UID), `userName` (default "Explorer"), `xp`, `energy`, `levelProgress`, `selectedGrade`, `selectedSubject`, and timestamps. No fields for real names, email addresses, phone numbers, or location data exist in the data model. Local saves are encrypted with AES (`EncryptionUtils`) using server-provided keys. The backend `authMiddleware.js` only verifies tokens — it does not store or log user identity information. | Pass |
| **NFR 2** | Having a user-friendly interface | Navigating through different tabs/section in the app. | Users can navigate between the world map, level select, gameplay, dashboard, settings, and leaderboard screens seamlessly. | The app uses Unity's `SceneManager` for navigation: `WinScreenController.cs` provides Restart, Next Level, and Menu buttons. `LevelSelectButton.cs` handles level selection with energy gating. `DashboardUIController.cs` provides a dedicated stats view. `Island_Portal.cs` and `WorldMapScroll.cs` manage world map navigation. The `OnboardingManager.cs` guides new users through initial setup. All game controllers extend `BaseLevelController.cs` providing a consistent gameplay UX across all mini-games. | Pass |
| **NFR 3** | Performance | Functionality of games without an internet connection. | All game levels load, play, score, and save progress without any internet connection. Loading times remain acceptable. | All game logic runs client-side in `BaseLevelController.cs` (timer, scoring, win/lose). `LocalDataManager.cs` saves to device storage and does not require network access. `CloudSyncManager.SyncProgress()` saves locally first and only attempts cloud sync if authenticated and online — failures are logged but do not block gameplay. Guest mode (`InitializeAsGuest()`) requires no network at all. LevelData is loaded from local ScriptableObjects (Unity `Resources` folder). | Pass |
| **NFR 4** | Accuracy | Answers in the games being correct according to the question. | All game questions have mathematically correct answers that match the Grade 3 Maths syllabus. | `QuestionItem` class defines `correctString` (the right answer) and `wrongAnswers` (distractors) per question — answers are authored in LevelData ScriptableObjects (e.g., `Level1_Numbers1-20.asset`). `TreasureRound` uses `RequiredTens => targetValue / 10` and `RequiredOnes => targetValue % 10` for mathematically verified place value decomposition. Power Station generates addition problems with `minTargetPower`/`maxTargetPower` ranges ensuring valid arithmetic. The scoring system in `BaseLevelController.cs` correctly distinguishes correct vs incorrect via `HandleCorrectAnswer()` and `HandleWrongAnswer()`. | Pass |
| **NFR 5** | Compatibility | The application working on all android devices where version is Android 7.1 + | The application installs and runs correctly on Android devices running Android 7.1 (API level 25) and above. | The Unity project targets Android via the Android build pipeline (confirmed by `Plugins` folder, `google-services.json`, Firebase SDK integration, and `.apk` build configuration). Unity 6+ supports Android API level 22+ by default, fully covering Android 7.1 (API 25). The `ExternalDependencyManager` plugin handles Android library resolution. Firebase Auth and Firestore SDKs used are compatible with Android 7.1+. The app uses standard Unity UI (Canvas, TextMeshPro) and audio systems which are fully compatible across all targeted Android versions. | Pass |